This group project aims to use clustering algorithm to group pickup and dropoff locations in NYC and calculate the time needed
for each cluster of locations to get to JFK and LGA airports during rush and non-rush hours. 
The algorithm we used was KMEANS with K = 10. 
